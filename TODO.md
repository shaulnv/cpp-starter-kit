# TODO

1. Merge Yoni's PR
2. Add support for compile to `wasm`
3. Add support for full static link of the CLI
4. Check windows build
5. Add devcontainer support
6. Add support for cross-build using `zigcpp`
7. Add env/setup.sh to install OS level deps
8. Add UV python package for deps
9. Add pre-commit plugins
10. Add automatic generation of python bindings to the library
    adapt [litgen](https://github.com/pthom/litgen_template) to this repo
11. Add packaging of the library as a PyPI package

